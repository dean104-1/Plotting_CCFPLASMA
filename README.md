# Plotting_CCFPLASMA
to clone use:  git clone https://github.com/dean104-1/Plotting_CCFPLASMA .
to add: git add <filename>
to commit: git commit -m "message"
to push: git push origin main
to pull: git pull origin main
